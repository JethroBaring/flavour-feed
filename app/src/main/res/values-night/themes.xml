<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FlavourFeed" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/flutter_sky</item>
        <item name="colorPrimaryVariant">@color/flutter_dark</item>
        <item name="colorOnPrimary">@color/flutter_dark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/flutter_sky</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/flutter_dark</item>
        <item name="android:navigationBarColor">@color/flutter_dark</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="MyBottomNavigationView" parent="@style/Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@color/flutter_dark</item>
    </style>
</resources>